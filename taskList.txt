open    Kern.Rezeption      修改左侧主导航菜单，使得在各页面的子页面中仍然可以高亮。
open    Kern.Rezeption      在消息队列-尚未加密中，添加“撰写”的快捷按钮。
open    Kern.Datenbank      增加CRUD的功能，即在gen函数和一种输出数据时的函数中，允许加密。
                            加密的密钥来自数据库本身（自产生时产生的密钥）。
